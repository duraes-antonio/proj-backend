language: node_js

node_js:
  - "12.13"

branches:
  only:
  - master

cache:
  yarn: false

addons:
  sonarcloud:
    organization: duraes-antonio-github
    token: ${SONAR_TOKEN}

install:
  - yarn install

script:
  - yarn test
  - sonar-scanner
